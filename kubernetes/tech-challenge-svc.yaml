apiVersion: v1
kind: Service
metadata:
  name: orderssvc
  namespace: "default"
  labels:
    app: ordersapp
spec:
  type: LoadBalancer
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: ordersapp
